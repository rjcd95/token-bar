{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-tray",
  "description": "Tray access for the main window",
  "windows": ["main"],
  "permissions": ["core:tray:default", "positioner:default", "process:allow-exit"]
}

